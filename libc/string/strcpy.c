/**
 * strcpy - a function that copies the string pointed to by src
 * @dest: string pointer
 * @src: buffer pointer
 *
 * Return: dest pointer
 */
char *strcpy(char *dest, const char *src)
{
	char *d = dest;
    while((*d++ = *src++))
	return (dest);
}